# CLAUDE.md — trpc/trpc

> Compiled by Groundtruth from the last 30 days of repository activity.
> Every rule below is an observed fact, not a guess.

## Observed activity (30 days)
- Pull requests: 48
- Issues: 13
- Active contributors: 5

## PR conventions (observed)
- Conventional-commit titles are the norm here (44/48 recent PRs match `type(scope): message`). Follow it.
- 19/48 recent PRs use GitHub closing keywords (`Fixes #N`). Only use a closing keyword when the diff actually resolves the issue — see SKILL.md: pr-autoclose-verification.

## Default branch
- `main`
